// Ensure the layout loads on the client side
export const ssr = false;

// This is a layout file to handle app route initialization
export function load() {
  return {
    // Add any data you want to pass to all app routes here
  };
}